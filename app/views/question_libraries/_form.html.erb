<%= form_with(model: question_library, local: true) do |form| %>
  <% if question_library.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(question_library.errors.count, "error") %> prohibited this question_library from being saved:</h2>

      <ul>
      <% question_library.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
